import React, {PropTypes}  from 'react';

const SignUpSuccess = () => {
  const successText = "Thanks for your interest in Helix, we\'ll be in touch!";

  return (
    <div id="signup-success">
      <p>{successText}</p>
    </div>
  );
};

export default SignUpSuccess;
