import React, {PropTypes} from 'react';
import {Link} from 'react-router-dom';

const CustomSlotTypeEmptyState = () => {
  return (
    <div className="emptyState">
      No custom slot types have been created.
    </div>
  );
};

export default CustomSlotTypeEmptyState;
